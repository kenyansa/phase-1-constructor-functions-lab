// objects
// Scooter constructor: should create a new scooter with three properties:
function Scooter(year, color, model) {
  this.year = year;
  this.color = color;
  this.model = model;
}
// objects
// Driver constructor:should create a new driver with three properties:
function Driver(name, age, experience) {
  this.name = name;
  this.age = age;
  this.experience = experience;
}
// objects
// PickupLocation constructor:should create a new PickupLocation with two properties:
function PickupLocation(address, city) {
  this.address = address;
  this.city = city;
}
